# 📦 הוראות חילוץ הגיבוי

## 📋 מה נמצא?

מצאתי שני קבצי גיבוי:
1. **`database (1).sql.gz`** - גיבוי מסד הנתונים
2. **`files (1).tar.gz`** - קבצי האתר (wp-content)

---

## 🔧 איך לחלץ?

### אפשרות 1: עם 7-Zip (מומלץ)

1. **התקן 7-Zip** (אם אין): https://www.7-zip.org/

2. **חלץ את הקבצים:**
   ```
   לחץ ימין על database (1).sql.gz → 7-Zip → Extract Here
   לחץ ימין על files (1).tar.gz → 7-Zip → Extract Here
   ```

3. **אחרי החילוץ:**
   - `database (1).sql` → העתק ל-`backup/database.sql`
   - `files (1).tar` → חלץ שוב (זה tar)
   - אחרי חילוץ ה-tar → העתק את `wp-content/` ל-`backup/`

---

### אפשרות 2: עם WinRAR

1. **לחץ ימין על הקבצים:**
   - `database (1).sql.gz` → Extract Here
   - `files (1).tar.gz` → Extract Here

2. **חלץ שוב את ה-tar:**
   - `files (1).tar` → Extract Here

3. **העתק את הקבצים:**
   - `database (1).sql` → `backup/database.sql`
   - כל התוכן מ-`files (1)/` → `backup/`

---

### אפשרות 3: אני יכול לעשות את זה אוטומטית!

אם תרצה, אני יכול לנסות לחלץ את הקבצים אוטומטית עם PowerShell.

**פשוט תגיד: "תחלץ את הקבצים"**

---

## 📂 איפה להעתיק אחרי החילוץ?

```
F:\cursor\files\higayonbarie-site\
├── backup/
│   ├── database.sql          ← database (1).sql
│   └── wp-content/          ← מתוך files (1).tar
│       ├── plugins/
│       ├── themes/
│       └── uploads/
```

---

## ✅ Checklist

- [ ] חילצתי את `database (1).sql.gz`
- [ ] חילצתי את `files (1).tar.gz`
- [ ] חילצתי את `files (1).tar`
- [ ] העתקתי את `database.sql` ל-`backup/`
- [ ] העתקתי את `wp-content/` ל-`backup/`

---

**עודכן:** 2025-12-07

