# ⚡ חילוץ מהיר - הוראות פשוטות

## 📦 מה יש לך?

2 קבצי גיבוי ב-`F:\cursor\`:
1. **`database (1).sql.gz`** - מסד הנתונים
2. **`files (1).tar.gz`** - קבצי האתר

---

## 🚀 איך לחלץ? (3 דקות)

### שלב 1: חלץ את הקבצים

**אם יש לך 7-Zip או WinRAR:**

1. **פתח את `F:\cursor\` ב-File Explorer**

2. **לחץ ימין על `database (1).sql.gz`:**
   - בחר: **Extract Here** (או Extract to "database (1).sql\")
   - תקבל: `database (1).sql`

3. **לחץ ימין על `files (1).tar.gz`:**
   - בחר: **Extract Here**
   - תקבל: `files (1).tar`

4. **לחץ ימין על `files (1).tar`:**
   - בחר: **Extract Here** (שוב!)
   - תקבל תיקייה: `files (1)` עם כל הקבצים

---

### שלב 2: העתק לפרויקט

**אחרי החילוץ, העתק:**

1. **`database (1).sql`** → 
   ```
   F:\cursor\files\higayonbarie-site\backup\database.sql
   ```

2. **כל התוכן מ-`files (1)/`** → 
   ```
   F:\cursor\files\higayonbarie-site\backup\
   ```
   
   (כלומר, אם יש `files (1)/wp-content/`, העתק את `wp-content/` ל-`backup/`)

---

## ✅ אחרי החילוץ

**המבנה צריך להיות:**
```
F:\cursor\files\higayonbarie-site\backup\
├── database.sql
└── wp-content/
    ├── plugins/
    ├── themes/
    └── uploads/
```

---

## 🆘 אין לך 7-Zip או WinRAR?

**הורד 7-Zip (חינם):**
- https://www.7-zip.org/
- התקן
- חזור לשלב 1

---

## 🎯 אחרי שהעתקת הכל

1. **פתח את הפרויקט ב-Cursor:**
   - `File` → `Open Folder`
   - בחר: `F:\cursor\files\higayonbarie-site`

2. **תגיד לי: "סיימתי"** ואני אארגן את הקבצים!

---

**עודכן:** 2025-12-07

